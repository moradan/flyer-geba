export default class Partido {
    fecha: string;
    localia: string;
    
    constructor(
        fecha: string,
        localia: string,
    ) {
        this.fecha = fecha;
        this.localia = localia;
    }
}
